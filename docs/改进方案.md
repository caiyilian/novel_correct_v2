# novel_correct_v2 — 改进方案

> 基于 Stage 18 完成后的现状分析，参考 novelSpeakerV4 的 token 跟踪和多 agent 协作经验。
> 讨论稿 — 待评审后进入实施。

---

## 一、现状分析

### 1.1 当前成果（Stage 18）

| 指标 | 值 |
|------|-----|
| 错误总数 | 151 |
| 已修正 | 143 |
| 跳过 | 8 |
| 失败 | 0 |
| 候选覆盖率 | 150/150 = 100% |
| 多轮收敛 | 130→69→60→59 (纯规则) |

### 1.2 当前架构（简化）

```
main.py (run_pipeline)
  ├── DetectorPipeline       → ErrorQueue
  ├── CandidateDecisionAgent
  │     ├── CandidateGenerator  → 生成候选
  │     ├── RulePrecheck       → 规则验证最低错误数
  │     └── LLM Fallback       → 调 qwen3:32b 做最后判断
  ├── DetectorPipeline       → 重新检测
  └── 收敛判断                → 2轮无进展停止
```

### 1.4 关键发现——检测器覆盖度盲区

Stage 18 宣称 151 错误 100% 覆盖，但对比答案 `data/answer_第1卷.txt`（由 novelSpeakerV4 人工整理，1349 对「」完美平衡），发现严重差距：

| 指标 | 原始文件 | 我们的修正后 | 答案目标 |
|------|---------|-------------|---------|
| 「 | 1205 | **1258** | **1349** |
| 」 | 1163 | **1283** | **1349** |
| 非标准符号残留 | 292 (128[, 164]) | **158 (65[, 93], 4弯引号)** | **0** |
| 不成对 | 42（「多） | 25（」多） | **0** |

**核心问题**：修正后文件中仍有 **158 个非标准符号**未被检测器发现。

### 1.3 存在的问题（续）

| # | 问题 | 具体表现 |
|---|------|----------|
| 0 | **检测器覆盖度盲区** | 修正后仍有 158 个非标准符号残留（65[, 93], 4弯引号），未被任何检测器捕获 |
| 1 | **无 token 跟踪** | 不知道每轮 LLM 调用消耗了多少 token，无法评估成本 |
| 2 | **无上下文监控** | qwen3:32b 有 40K context，但从不检查用了多少，可能浪费或溢出 |
| 3 | **Verifier 被绕过** | 当前路线的候选由规则生成，直接被应用，未经过 Verifier |
| 4 | **单 agent 瓶颈** | CandidateDecisionAgent 集成了生成+判断+应用+验证，职责揉在一起 |
| 5 | **无多轮质量监控** | 只有错误数统计，没有"修正质量"的维度（如语义保留度） |
| 6 | **无结构化日志** | 只看 correction_report.json 不能分析趋势、瓶颈、回归 |

---

## 二、改进方向

### 方向 A — Token 跟踪与上下文监控

**目标**：每次 LLM 调用都记录 token 消耗，监控上下文窗口利用率。

#### 当前问题

`ChatResult` 虽然存了 `raw` 字段（完整 payload），但：
1. 没有提取 `usage.prompt_tokens` / `usage.completion_tokens`
2. 没有暴露 `prompt_eval_count` / `eval_count`（Ollama API 返回的字段名）
3. 没有任何地方记录或汇总 token 消耗
4. 没有任何地方计算上下文窗口使用率

#### 参考 novelSpeakerV4

```python
# novelSpeakerV4 的 token 跟踪模式：
text, pec, ec, tool_calls = call_ollama(...)  # 每个 LLM 调用都返回 token 数
total_tokens = pec + ec                        # 立即累加
log_agent(..., pec=pec, ec=ec, total_tokens=...)  # 记录到每轮日志

# 上下文监控：
TOKEN_BUDGET = 18000  # 预算控制
if cumulative_tokens >= TOKEN_BUDGET:
    print(f"Token budget reached ({cumulative_tokens} >= {TOKEN_BUDGET}), stopping")
```

#### 改进方案

**1. 增强 ChatResult，附带 token 信息**

```python
# src/model/client.py
@dataclass(frozen=True)
class TokenUsage:
    prompt_tokens: int = 0
    completion_tokens: int = 0
    total_tokens: int = 0

@dataclass(frozen=True)
class ChatResult:
    content: str = ""
    tool_calls: list[ToolCall] = field(default_factory=list)
    raw: dict[str, Any] = field(default_factory=dict)
    usage: TokenUsage = field(default_factory=TokenUsage)  # 新增
```

**2. 在 _parse_chat_result 中解析 usage**

Ollama API 返回两种格式，都需要兼容：

```python
# Ollama /api/chat 格式
{
  "prompt_eval_count": 123,
  "eval_count": 456
}

# OpenAI-compatible /v1/chat/completions 格式
{
  "usage": {
    "prompt_tokens": 123,
    "completion_tokens": 456,
    "total_tokens": 579
  }
}
```

增强 `_parse_chat_result`：
```python
def _parse_chat_result(self, payload: dict) -> ChatResult:
    # ... 现有代码解析 content、tool_calls ...
    
    # 新增：提取 token 使用
    usage = TokenUsage()
    # Ollama 原生格式
    pec = payload.get("prompt_eval_count", 0)
    ec = payload.get("eval_count", 0)
    if pec or ec:
        usage = TokenUsage(prompt_tokens=pec, completion_tokens=ec, total_tokens=pec+ec)
    else:
        # OpenAI 兼容格式
        u = payload.get("usage", {}) or {}
        usage = TokenUsage(
            prompt_tokens=u.get("prompt_tokens", 0),
            completion_tokens=u.get("completion_tokens", 0),
            total_tokens=u.get("total_tokens", 0),
        )
    
    return ChatResult(content=..., tool_calls=..., raw=payload, usage=usage)
```

**3. 创建 TokenTracker 类**

```python
# src/model/token_tracker.py
@dataclass
class TokenRecord:
    timestamp: float
    source: str           # 哪个 agent/环节
    error_id: str         # 关联的错误 ID
    error_type: str       # 错误类型
    prompt_tokens: int
    completion_tokens: int
    total_tokens: int
    context_window_pct: float  # 上下文窗口使用率

class TokenTracker:
    """全局 token 消耗追踪器"""
    
    def __init__(self, context_limit: int = 40960):
        self._records: list[TokenRecord] = []
        self._context_limit = context_limit
        self._round_totals: dict[int, int] = {}  # round → total_tokens
    
    def record(self, source: str, error_id: str, error_type: str,
               usage: TokenUsage) -> TokenRecord:
        record = TokenRecord(
            timestamp=time.time(),
            source=source,
            error_id=error_id,
            error_type=error_type,
            prompt_tokens=usage.prompt_tokens,
            completion_tokens=usage.completion_tokens,
            total_tokens=usage.total_tokens,
            context_window_pct=round(usage.prompt_tokens / self._context_limit * 100, 1),
        )
        self._records.append(record)
        return record
    
    @property
    def total_tokens(self) -> int:
        return sum(r.total_tokens for r in self._records)
    
    @property
    def total_cost_estimate(self) -> float:
        """估算成本（仅供参考，qwen3:32b 约 ¥4/1M tokens）"""
        return self.total_tokens / 1_000_000 * 4.0
    
    def to_json(self) -> dict:
        return {
            "context_limit": self._context_limit,
            "total_records": len(self._records),
            "total_tokens": self.total_tokens,
            "estimated_cost_cny": round(self.total_cost_estimate, 2),
            "records": [dataclasses.asdict(r) for r in self._records],
        }
    
    def save(self, path: str = "output/token_usage.json"):
        with open(path, "w", encoding="utf-8") as f:
            json.dump(self.to_json(), f, ensure_ascii=False, indent=2)
```

**4. 日志中集成 token 记录**

每个 error 修复完成后，在 correction_report.json 中附带 token 消耗：

```json
{
  "error_id": "e-0042",
  "token_usage": {
    "prompt_tokens": 2340,
    "completion_tokens": 85,
    "total_tokens": 2425,
    "context_window_pct": 5.7
  }
}
```

### 方向 B — 多 Agent 协作架构

**目标**：将单 agent 拆分为多个职责清晰的子 agent，各自独立优化。

#### 设计原则（来自 novelSpeakerV4 的经验）

1. **每个 agent 只有一项核心职责**
2. **agent 间通过结构化数据通信（不是自由对话）**
3. **每个 agent 的 prompt 极致精简**
4. **有独立的 memory 层管理上下文**

#### 新的 Agent 架构

```
┌─────────────────────────────────────────────────┐
│                 Boss (Orchestrator)               │
│  管理流程、调度 agent、判断终止条件                │
├─────────────────────────────────────────────────┤
│                                                    │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐     │
│  │ Detector   │  │ Corrector  │  │ Verifier   │     │
│  │ Agent      │  │ Agent      │  │ Agent      │     │
│  │            │  │            │  │            │     │
│  │ 只检测     │  │ 只生成     │  │ 只验证     │     │
│  │ 错误       │  │ 候选并判断  │  │ 修正质量   │     │
│  └───────────┘  └───────────┘  └───────────┘     │
│                                                    │
│  ┌───────────┐  ┌───────────┐                     │
│  │ Quality    │  │ Reporter   │                     │
│  │ Agent      │  │ Agent      │                     │
│  │            │  │            │                     │
│  │ 终局质量   │  │ 生成报告   │                     │
│  │ 验收       │  │ 含 token   │                     │
│  └───────────┘  └───────────┘                     │
│                                                    │
│  ┌──────────────────────────────────────┐          │
│  │ TokenTracker (跨 agent，只读)          │          │
│  │ 全局记录所有 LLM 调用的 token 消耗     │          │
│  └──────────────────────────────────────┘          │
└─────────────────────────────────────────────────┘
```

#### Agent 详细职责

##### 1. Detector Agent（改造现有的 DetectorPipeline）

当前已有，但可以增加一步：**错误分类与优先级排序**。

```python
class DetectorAgent:
    """
    职责：运行规则检测器 → 输出带优先级的 ErrorQueue
    
    改动点：
    1. 对每个错误计算"置信度"（consecutive=高, wrong_symbol=中, long_dialogue=低）
    2. 按置信度排序（高级优先处理）
    3. 附加"关联错误"信息（相邻位置的不同检测器结果可能相关）
    4. 记录检测耗时、覆盖范围
    """
```

##### 2. Corrector Agent（拆分现有的 CandidateDecisionAgent）

```python
class CorrectorAgent:
    """
    职责：接收 ErrorRecord → 生成候选 → 送 LLM 判断 → 应用修正
    
    与原 CandidateDecisionAgent 的区别：
    1. 只负责"修正"这一个步骤，不关心检测和验证
    2. 修正后返回 (ErrorRecord, AppliedCandidate, TokenUsage)
    3. 不直接写 ErrorQueue，返回结果由 Boss 处理
    4. LLM 调用的 token 消耗通过 TokenTracker 记录
    
    子模块：
    - CandidateGenerator（已有，增强）
    - LLM Judge（判断选哪个候选，增强 prompt 带 token 预算提醒）
    """
```

##### 3. Verifier Agent（已有，但需要加强启用）

当前 Verifier 在 candidate-decision 路径中几乎不用（候选由规则生成，认为不需要验证）。但这是错误的——**规则生成也可能引入新错误**。

```python
class VerifierAgent:
    """
    职责：验证一个修正是否真的修复了问题，且没有引入新问题
    
    验证维度：
    1. 符号平衡：修正后「」数量是否平衡
    2. 无连续符号：修正后没有新引入的「「或」」
    3. 长度变化合理：替换前后长度变化在预期范围内
    4. 语义保留（可选 LLM 验证）：修正没有改变原文意思
    
    输出：VerifierVerdict (pass/fail/uncertain)
    
    对 rule-precheck 路径的改动：
    当前 rule-precheck 已经跑了一遍完整检测（验证错误数降低），
    但缺少单次修正的验证。建议：
    - rule-precheck 通过的候选仍需经过快速规则验证（1-3）
    - rule-precheck 不通过的候选走 LLM 判断 → 验证
    """
```

##### 4. Quality Agent（新增）

```python
class QualityAgent:
    """
    职责：每轮结束后做全文质量评估，不修正，只评估
    
    评估维度（参考开发方案中的"硬性指标"）：
    1. 「」数量平衡
    2. 无连续相同符号
    3. 无非标准对话符号
    4. 无超长对话
    
    额外维度：
    5. 错误数收敛趋势（本轮 vs 上轮）
    6. token 消耗趋势（是否过度消耗）
    
    输出：QualityReport → Boss 判断是否终止或继续
    """
```

##### 5. Boss (Orchestrator) — 升级现有的 run_pipeline

```python
class Boss:
    """
    职责：编排整个纠错流程，调度子 agent，判断终止
    
    流程：
    Phase 0: TextLoader 加载文本
    Phase 1: DetectorAgent.run() → ErrorQueue
    Phase 2: multi-round correction loop
      - CorrectorAgent.run(error) → fixed/skipped/failed
      - VerifierAgent.verify(correction) → pass/fail/uncertain
      - TokenTracker.record(...)  # 每步都记录
    Phase 3: QualityAgent.evaluate(text) → QualityReport
    Phase 4: 收敛判断（同现有逻辑）
    Phase 5: ReporterAgent.generate() → 含 token 消耗的全量报告
    
    新增功能：
    - 每轮结束后打印 token 统计摘要
    - 上下文使用率预警（>80% 时提醒）
    - 支持 --token-report 参数单独输出 token 日志
    """
```

### 方向 C — 上下文窗口充分利用

**目标**：让每次 LLM 调用都能充分利用 qwen3:32b 的 40K 上下文窗口。

#### 当前问题

LLM fallback 的 calls 非常简短：

```
User prompt: 200-300 行 JSON（描述错误 + 候选列表）
System prompt: 5-8 行规则
LLM response: 1 行 JSON 或 tool call
```

上下文使用率估算：`(300 + 5 + 50) / 40960 ≈ 0.9%`

这意味着：
- 模型只看到极少量上下文
- 无法进行深度语义理解
- 只能靠候选生成器的质量，LLM 沦为"yes/no 分类器"

#### 改进方案

**1. 为 LLM 调用提供更丰富的上下文**

```python
def _user_prompt(self, error, candidates):
    return {
        "error": {
            "error_id": error.error_id,
            "error_type": error.error_type,
            "line_number": error.line_number,
        },
        "context": {
            "before": error.context_before[-800:],   # 800 chars before
            "after": error.context_after[:800],      # 800 chars after
            "full_sentence": extract_sentence(error),  # 完整句子上下文
        },
        "candidates": [...],
        "surrounding_dialogues": extract_nearby_dialogues(error, n=3),  # 附近3段对话
    }
```

目标：每次 LLM 调用使用 **3,000-8,000 tokens** 的上下文，占 40K 窗口的 **7-20%**。
既不是 1%（浪费），也不是 100%（溢出风险）。

**2. 对复杂错误（long_dialogue / missing_bracket）提供更多上下文**

这些错误类型需要理解全文角色关系，可以给 10K+ 上下文。

```python
CONTEXT_BUDGET = {
    "consecutive": (200, 200),        # 短上下文，符号修复足够
    "wrong_symbol": (500, 500),       # 中等上下文，判断是否为对话符号
    "unpaired": (500, 500),
    "long_dialogue": (2000, 2000),    # 长上下文，需要看完整段
    "missing_bracket": (2000, 2000),
}
```

**3. 上下文使用率预警**

```python
class ContextMonitor:
    def check(self, usage_pct: float, error_type: str):
        if usage_pct < 2.0:
            # 警告：上下文利用率过低，可能应该给更多上下文
            pass
        elif usage_pct > 80.0:
            # 严重警告：上下文即将溢出，可能需要分段
            pass
```

### 方向 D — 验证与质量保障

#### D1. 启用 Verifier 对规则候选的验证

当前 rule-precheck 确认"整体错误数降低"就直接应用候选，缺少单次修正质量检查。

建议：

```python
def _apply_with_verify(self, error, candidate):
    """应用候选 + 验证"""
    # 1. 保存修正前的快照
    snapshot = self._text.text
    
    # 2. 应用修正
    self._text.replace_range(candidate.start_offset, candidate.end_offset, candidate.replacement)
    
    # 3. 快速规则验证（不调 LLM）
    quick_checks = self._quick_verify(error, candidate)
    if not quick_checks.all_pass():
        self._text.text = snapshot  # 回滚
        return AgentResult(verdict="fail", reason=quick_checks.failure_reason())
    
    # 4. (可选) LLM 语义验证
    if self._llm_fallback:
        verdict = self._verifier.verify(error, candidate)
        if verdict != "pass":
            self._text.text = snapshot  # 回滚
            return AgentResult(verdict=verdict, ...)
    
    # 5. 确认修正成功
    return AgentResult(verdict="pass", ...)
```

#### D2. 多轮质量报告

每轮结束后产出质量卡片：

```
Round 2/5 — Quality Card
┌─────────────────────────────────────┐
│ Errors:  130 → 69 (↓47%)           │
│ Fixed:   67  Skipped: 2  Failed: 0  │
│ Tokens:  12,345  (¥0.05)            │
│ Context: avg 3.2%, max 8.1%         │
│ Bracket balance: ✅                  │
│ No consecutive: ✅                   │
│ No wrong symbols: ❌ (2 remaining)  │
└─────────────────────────────────────┘
```

#### D3. 答案验证（对第一卷）

已复制 novelSpeakerV4 的 `data/answer_第1卷.txt` 作为答案。

验证方式：

```bash
python tools/verify_against_answer.py \
  output/corrected_第1卷.txt \
  data/answer_第1卷.txt \
  --ignore-whitespace
```

输出：
- 精确逐行匹配率
- 忽略空白后的匹配率
- 符号修正的一致性
- 差异行列表（供人工审核）

> **注意**：答案文件**只能用于最终验证**，绝不可以作为 LLM 的输入或训练数据。

---

### 方向 E — 检测器覆盖度扩展（先行）

**目标**：消除 158 个残留非标准符号的检测盲区，使修正后的「」数量逼近答案的 1349 对。

#### 根因分析

结合上文的对比数据，158 个残留符号的漏检原因分类：

| 漏检模式 | 估计数量 | 根因 |
|---------|---------|------|
| `「」` 内部嵌套的 `[` `]` | ~70 | WrongSymbolDetector 的嵌套保护过滤：`count_nested()` 检测到在 `「」` 内部就跳过 |
| 短注释型 (`[1]` `[注]`) | ~40 | SmartSkip 逻辑（`_is_likely_comment()`）误判为注释跳过 |
| 不成对残留（修复后产生的新不平衡）| ~25 | 修复一个括号导致另一侧失衡，但检测器认为"已处理"就不再抓 |
| 弯引号 `""` | ~4 | 弯引号检测覆盖不全 |
| 其他（分页标记、数字编号等）| ~19 | 特殊格式，当前检测器无匹配模式 |

#### 解决策略

**1. 修复嵌套保护逻辑**

当前代码（`src/detector/wrong_symbol.py` 中的 `count_nested()`）：
```python
def count_nested(self, text, pos):
    # 统计从 pos 向前/向后还有多少层「」，
    # 如果 > 0 说明在「」内部，跳过检测
```

问题：`「...[...]...」` 中，`[...]` 内部的 `[]` 被正确跳过（因为它在 `「」` 外部），但如果是 `「他说这是[重要的事]」`，`[` 在 `「」` 内部——虽然它仍然是需要修正的符号。

建议改成：**取消 `「」` 内部的跳过**。所有非标准符号都应被标记，无论是否在 `「」` 内部。但如果在一个大段 `「」` 内部出现成对的 `[]`，可以降低优先级（P2 而非 P1），让 LLM 判断是否真的是注释。

**2. 缩小 SmartSkip 范围**

当前 SmartSkip 跳过所有 `[数字]` 和 `[短文本]`，但小说对话中 `[重要的事]` 这样的短文本也需要修正。

建议改成：**只跳过明确的注释标记**（如 `[1]`、`[注]`、`[插图]` 等固定模式），对任意其他 `[中文文本]` 不再跳过。

**3. 增加跨轮平衡检测**

新增一个检测器 `BracketBalanceDetector`（或集成到现有 UnpairedDetector）：
- 每轮修正结束后，检查「」总数差
- 如果差异 > 可接受阈值（如 > 5），追加扫描上次修正造成的不平衡
- 生成新的 ErrorRecord 让下一轮处理

**4. 答案驱动的回归测试**

对所有 158 个残留符号，逐个分析它们为什么没被检测：

```python
# tools/audit_detector_gap.py
# 读取修正后文件 → 逐一检查每个残留符号
# 对每个符号运行 DetectorPipeline 查看是否命中
# 分类：nested / smart_skip / type_not_covered / offset_mismatch
# 输出分析报告
```

#### 验收标准

1. `data/answer_第1卷.txt` 的 1349 对 `「」` 全部对齐（我们的修正后输出 `「` ≥ 1340）
2. 修正后文件非标准符号残留 < 10
3. 回归测试全部通过
4. 新增 `tests/test_stage19_coverage.py` — 20+ 项

---

## 三、实施阶段划分（12 阶段，每个 ≤ 80 行改动）

每个阶段都是独立、可验证、可回滚的。改动量控制在 30-80 行，验收标准明确可测。

### 第 1 梯队：检测器覆盖度修复（核心准确率）

---

### Stage 19a — 漏检审计工具

**目标**：精确了解 158 个残留符号各自为什么没被检测到。

**改动**：新建 `tools/audit_detector_gap.py`
- 读取修正后文件中的残留非标准符号
- 对每个符号逐一运行 DetectorPipeline，检查是否命中
- 输出四分类：`nested` / `smart_skip` / `type_not_covered` / `unknown`
- 产生精确的 158 条明细表

**改动量**：~80 行新文件

**验收**：
```
python tools/audit_detector_gap.py output/corrected_第1卷.txt
→ 输出 158 条记录，每条带 {符号, 位置, 漏检原因}
```

---

### Stage 19b — 修复嵌套保护过严

**改动**：`src/detector/wrong_symbol.py`
- 当前：`「...[...]...」` 内部的 `[]` 被 `count_nested()` 跳过
- 改为：取消 `「」` 内部的跳过，所有非标准符号都标记
- 保留：大段 `「」` 内部成对的 `[文本]` 降为 P2 优先级
- 加标志：`is_nested: bool` 标记是否在 `「」` 内部

**改动量**：~30 行

**验收**：
```
测试「这是[测试]」→ 检测到 `[` 和 `]`，is_nested=True
回归：原有 WrongSymbolDetector 测试全部通过
```

---

### Stage 19c — 修复 SmartSkip 过宽

**改动**：`src/detector/wrong_symbol.py` 的 `_is_likely_comment()`
- 当前：跳过所有 `[数字]`、`[短中文文本]`
- 改为：只跳过固定的注释模式——`[1]`、`[注]`、`[插图]`、`[图]`、`[表]`、`[序号]` 等
- 对其他任意 `[中文文本]` 不再跳过
- 注释模式列表配置在模块常量

**改动量**：~20 行

**验收**：
```
测试：[1] → 跳过 ✅
测试：[重要的事] → 不跳过 ✅
回归：原测试不变
```

---

### Stage 19d — 修复弯引号漏检

**改动**：`src/detector/wrong_symbol.py`
- 当前：弯引号 `""`（U+201C/U+201D）检测覆盖不全，修正后仍有 4 残留
- 定位并修复 TARGET_SYMBOLS 映射
- 补丁：扫描到 `"` 但附近无配对的 `"` 时也标记为错误

**改动量**：~15 行

**验收**：
```
扫描原文件：识别到全部 11 个弯引号（之前只识别部分）
回归通过
```

---

### Stage 19e — 候选生成器补齐

**改动**：`src/agent/candidates.py`
- Stage 19b/c 修复后，WrongSymbolDetector 会多检出约 100+ 新错误
- 确保这些新错误的候选生成逻辑完整
- 特别：`wrong_symbol` 候选生成器覆盖 `「」` 内部 `[]` → `「」` 替换

**改动量**：~50 行

**验收**：
```
新检出的 wrong_symbol 错误中，≥95% 能生成候选
回归：tests/test_stage18.py 全部通过
```

---

### Stage 19f — 全管线验证

**改动**：纯运行，无代码修改

**验收**：
```
python main.py data/ori_story/第1卷.txt --llm-decision-fallback
→ 修正后非标准符号残留 < 10（从 158 降低）
→ 修正后「」≥ 1340（目标 1349）
→ 答案匹配率 > 90%
```

---

### 第 2 梯队：观测能力（Token 跟踪 + 上下文）

---

### Stage 20a — TokenUsage 解析

**改动**：`src/model/client.py`
- `ChatResult` 增加 `usage: TokenUsage` 字段
- `_parse_chat_result()` 解析 Ollama 响应的 `prompt_eval_count`、`eval_count`
- 同时兼容 OpenAI 格式的 `usage.prompt_tokens`

**改动量**：~40 行

**验收**：
```
每次 model.chat() 后，result.usage.total_tokens > 0
mock 响应测试通过
```

---

### Stage 20b — TokenTracker + 日志

**改动**：新增 `src/model/token_tracker.py`，修改 `main.py`
- TokenTracker 类：存储每步 LLM 调用的 token 记录
- 集成到 pipeline：每调一次 LLM 就 record 一次
- 运行结束后输出 `output/token_usage.json`

**改动量**：~100 行新文件 + ~20 行集成

**验收**：
```
运行后 output/token_usage.json 存在
包含每轮 LLM 调用的 prompt_tokens / completion_tokens
```

---

### Stage 20c — 上下文预算

**改动**：`src/agent/prompts.py`、`src/agent/decision.py`
- 按错误类型设置不同上下文长度
  - `consecutive`: 200+200 chars
  - `long_dialogue`: 2000+2000 chars
- `token_usage.json` 中上下文利用率从 <1% 提升到 5-20%

**改动量**：~30 行

**验收**：
```
long_dialogue 的 LLM 调用，context_window_pct ≥ 5%
token_usage.json 可查每类错误的平均上下文使用率
```

---

### 第 3 梯队：质量保障

---

### Stage 21a — Verifier 验证规则候选

**改动**：`src/agent/decision.py`、`src/verifier/agent.py`
- 当前：rule-precheck 的候选不经过 Verifier
- 改为：rule-precheck 候选也要经过快速规则验证（符号平衡、无新连续符号）
- rule-precheck 失败 → 降级为 LLM 判断

**改动量**：~50 行

**验收**：
```
所有 fix 都经过 Verifier 验证
无新增 Verifier 误判
回归测试全部通过
```

---

### Stage 22a — 答案验证工具

**改动**：新建 `tools/verify_against_answer.py`
- 逐行匹配率（精确 / 忽略空白）
- 符号匹配度
- 差异行列表

**改动量**：~80 行新文件

**验收**：
```
python tools/verify_against_answer.py output/corrected_第1卷.txt data/answer_第1卷.txt
→ 输出匹配率、差异行
```

---

### Stage 23a — CLI 质量报告

**改动**：`main.py`
- `--report` 参数输出增强版质量报告（含 token 消耗、上下文利用率、硬性指标状态）
- 终端彩色输出：✅/❌ 每项硬性指标

**改动量**：~60 行

**验收**：
```
python main.py data/ori_story/第1卷.txt --report
→ 输出质量卡片（错误分布、token 总消耗、上下文平均利用率）
```

---

### Stage 19a-19f 以外的第 1 卷纯规则演进路径

```
原始   1205「 1163」 → 42 不成对 + 292 非标准符号
Stage 18 1258「 1283」 → 残留 158 非标准符号（检测器盲区）
Stage 19 目标 1340+「 → 残留 < 10 非标准符号
答案   1349「 1349」 → 完美
```

---

## 四、验证计划

### 回归测试

每次 Stage 完成后：
```bash
python -m pytest tests/ -v
```

新增测试：
- Stage 19b-d: `tests/test_wrong_symbol_extended.py`
- Stage 19e: （复用现有的 test_stage18.py）
- Stage 20a: `tests/test_token_usage.py`
- Stage 20b: `tests/test_token_tracker.py`
- Stage 20c: `tests/test_context_budget.py`
- Stage 21a: `tests/test_verifier_extended.py`

### 性能基线

| 指标 | 当前 | Stage 19 目标 | 最终目标 |
|------|------|--------------|---------|
| 非标准符号残留 | 158 | < 10 | < 5 |
| 修正后「」总数（目标 1349） | 1258+1283 | ≥ 1340 | 1349 |
| 候选覆盖率 | 100% | 100% | 100% |
| Token 跟踪 | ❌ | ❌ | ✅ |
| 上下文利用率 | < 1% | < 1% | 5-20% |
| Verifier 全覆盖 | ❌ | ❌ | ✅ |
| 答案匹配率 | 未知 | > 90% | > 95% |

---

## 五、讨论点（待确认）

1. 先做 Stage 19a（漏检审计）还是直接 19b（修嵌套保护）？
   - 19a 能看到精确分类 → 但 19b 最可能解决 70/158 漏检
   - 推荐：先 19a，后 19b/c/d（审计驱动修复）

2. Token 跟踪（Stage 20a-b）要不要等检测器修复完再做？
   - 不需要。20a-b 不改检测器逻辑，独立基础设施
   - 可以 Stage 19a 之后并行

3. 答案文件 `data/answer_第1卷.txt` 是否绝对正确？
   - 影响 Stage 22a 验证工具的判据设计